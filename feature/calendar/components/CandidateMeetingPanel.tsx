import Link from "next/link";
import type { Candidate360State } from "@/feature/candidate-360/models/Candidate360State";
export function CandidateMeetingPanel({ candidate }: { candidate: Candidate360State }) { return <section aria-label="Meetings and Calendar" className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.2em] text-blue-600">Next Meeting</p>{candidate.nextMeeting ? <><h2 className="mt-1 text-xl font-black">{candidate.nextMeeting.title}</h2><p className="mt-1 text-sm text-slate-600">{candidate.nextMeeting.dateLabel} · {candidate.nextMeeting.timeLabel} · {candidate.nextMeeting.locationLabel}</p></> : <p className="mt-2 text-sm text-slate-600">No upcoming meeting scheduled.</p>}</div><div className="flex gap-2">{candidate.nextMeeting && <Link href={`/crm/calendar?event=${candidate.nextMeeting.id}`} className="rounded-xl border px-4 py-2 text-sm font-black">View Meeting</Link>}<Link href={`/crm/calendar?candidate=${candidate.id}`} className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-black text-white">Add Meeting</Link></div></div></section>; }
