# Integration boundary

This package contains the typed candidate-intelligence model used by the newer
AI, evidence, and reasoning pipeline. The current CRM application model and its
screen-facing engines remain canonical in `feature/intelligence`. Convert
between the two models through explicit adapters or merge services rather than
mixing both model families in presentation components.
