import Link from"next/link";import type{ProductionDiscoverySession}from"@/feature/discovery/production/types";
export function ProductionDiscoverySummary({candidateId,session}:{candidateId:string;session:ProductionDiscoverySession|null}){const intelligence=session?.currentIntelligence;const label=intelligence?.readiness==="ready-for-brand-strategy"?"Ready for Brand Strategy":session?.status==="completed"?"Discovery Complete":session?.status==="in-progress"?"Discovery In Progress":"Discovery Not Started";return <section className="rounded-2xl border bg-white p-6 shadow-sm"><p className="text-xs font-black uppercase tracking-[.2em] text-teal-600">Discovery</p><div className="mt-2 flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-black">{label}</h2><p className="mt-1 text-sm text-slate-600">{intelligence?.consultantBrief.nextAction??"Use assessment priorities to begin consultant-led Discovery."}</p></div><Link href={`/crm/${candidateId}/discovery`} className="rounded-xl bg-teal-700 px-4 py-2.5 text-sm font-black text-white">Open Discovery Workspace</Link></div>{intelligence&&<div className="mt-5 grid gap-4 md:grid-cols-2"><div><h3 className="text-sm font-black">Current understanding</h3><p className="mt-1 text-sm text-slate-600">{intelligence.consultantBrief.currentUnderstanding}</p></div><div><h3 className="text-sm font-black">Unresolved areas</h3><ul className="mt-1 list-disc pl-5 text-sm text-slate-600">{intelligence.unresolvedQuestions.slice(0,3).map(x=><li key={x}>{x}</li>)}</ul></div></div>}</section>}
