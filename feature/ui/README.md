# Canonical UI boundary

This package owns reusable product UI primitives and content-layout helpers.
The authenticated application shell, global sidebar, top bar, and product
navigation are canonical in `feature/layout`. The sidebar retained in this
package is a compatibility component and must not become a second navigation
source.
