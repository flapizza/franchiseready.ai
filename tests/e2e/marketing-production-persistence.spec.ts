import { expect, test } from "@playwright/test";
const url=required("NEXT_PUBLIC_SUPABASE_URL"),key=required("SUPABASE_SERVICE_ROLE_KEY"),email=required("PACK2A_TEST_EMAIL"),password=required("PACK2A_TEST_PASSWORD");
test.setTimeout(180_000);
test.beforeAll(async()=>{expect(process.env.PERSISTENCE_MODE).toBe("supabase");const response=await fetch(`${url}/auth/v1/admin/users`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({email,password,email_confirm:true})});expect(response.status).toBe(200)});
test("production composition persists Segment and Campaign draft",async({page})=>{await page.goto("/login");await page.getByLabel("Email address").fill(email);await page.getByLabel("Password").fill(password);await page.getByRole("button",{name:"Sign in"}).click();await expect(page).toHaveURL(/\/$/);await page.goto("/crm");await expect(page).toHaveURL(/\/onboarding$/);await page.getByLabel("Organization name").fill("Pack 4 Persistence");await page.getByLabel("Your display name").fill("Pack 4 Consultant");await page.getByRole("button",{name:"Create workspace"}).click();await expect(page).toHaveURL(/\/crm$/);await page.goto("/crm/segments/new");await page.getByLabel("Segment name").fill("Persistent Prospects");await page.getByRole("button",{name:"Save Segment"}).click();await expect(page).toHaveURL(/\/crm\/segments\/seg_/);await page.reload();await expect(page.getByRole("heading",{name:"Persistent Prospects"})).toBeVisible();await page.getByRole("link",{name:"Create Campaign from Segment"}).click();await page.getByLabel("Campaign name").fill("Persistent Draft");await page.getByLabel("Subject line").fill("Hello");await page.getByLabel("Sender display name").fill("Pack 4 Consultant");await page.getByLabel("Reply-to address").fill(email);await page.getByLabel("Body content").fill("Hello {{first_name}}");await page.getByRole("button",{name:"Save Draft"}).click();await expect(page).toHaveURL(/\/crm\/campaigns\/camp_/);await page.reload();await expect(page.getByRole("heading",{name:"Persistent Draft"})).toBeVisible()});
function required(name:string){const value=process.env[name];if(!value)throw new Error(`${name} is required`);return value}
