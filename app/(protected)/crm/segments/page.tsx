import Link from'next/link';import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';export default async function SegmentsPage(){const r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();const segments=await r.composition.dependencies.marketing.listSegments();return <div className="space-y-6"><header className="flex items-end justify-between"><div><h1 className="text-3xl font-black">Dynamic Segments</h1><p className="mt-2 text-slate-600">Live, organization-scoped audiences calculated from persisted Contact data.</p></div><Link href="/crm/segments/new" className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Create Segment</Link></header><div className="grid gap-4 md:grid-cols-2">{segments.map(x=><Link key={x.id} href={`/crm/segments/${x.id}`} className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-xl font-black">{x.name}</h2><p className="mt-2 text-sm text-slate-600">{x.description||`${x.criteria.rules.length} live rules`}</p></Link>)}</div>{!segments.length&&<p className="rounded-2xl border bg-white p-8 text-slate-600">Create a Segment to calculate a reusable audience.</p>}</div>}
