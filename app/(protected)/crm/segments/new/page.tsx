import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';import{SegmentBuilder}from'@/feature/marketing/components/SegmentBuilder';export default async function NewSegment(){const r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();return <div className="space-y-5"><h1 className="text-3xl font-black">Create Dynamic Segment</h1><SegmentBuilder options={await r.composition.dependencies.marketing.options()}/></div>}
