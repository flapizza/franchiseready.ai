import Link from'next/link';import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';import{SegmentBuilder}from'@/feature/marketing/components/SegmentBuilder';export default async function SegmentDetail({params}:{params:Promise<{id:string}>}){const{id}=await params,r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();const repo=r.composition.dependencies.marketing,[segment,options]=await Promise.all([repo.getSegment(id),repo.options()]);if(!segment)notFound();return <div className="space-y-5"><header className="flex items-end justify-between"><div><Link href="/crm/segments" className="text-sm font-bold text-slate-500">Back to Segments</Link><h1 className="mt-3 text-3xl font-black">{segment.name}</h1></div><Link href={`/crm/campaigns/new?audience=segment&id=${segment.id}`} className="rounded-xl bg-slate-900 px-5 py-3 font-bold text-white">Create Campaign from Segment</Link></header><SegmentBuilder segment={segment} options={options}/></div>}
