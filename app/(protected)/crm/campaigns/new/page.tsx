import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';import{CampaignEditor}from'@/feature/marketing/components/CampaignEditor';export default async function NewCampaign({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const q=await searchParams,r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();const type=q.audience==='list'?'list':'segment',id=typeof q.id==='string'?q.id:'';return <div className="space-y-5"><h1 className="text-3xl font-black">Create Campaign</h1><CampaignEditor options={await r.composition.dependencies.marketing.options()} initialAudience={{type,id}}/></div>}
