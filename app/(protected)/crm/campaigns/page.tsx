import Link from'next/link';import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';export default async function CampaignsPage(){const r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();const campaigns=await r.composition.dependencies.marketing.listCampaigns();return <div className="space-y-6"><header className="flex items-end justify-between"><div><h1 className="text-3xl font-black">Marketing Campaigns</h1><p className="mt-2 text-slate-600">Compose governed email campaigns and prepare them for future delivery.</p></div><div className="flex gap-2"><Link href="/crm/segments" className="rounded-xl border px-5 py-3 font-bold">Segments</Link><Link href="/crm/campaigns/new" className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">New Campaign</Link></div></header><div className="space-y-3">{campaigns.map(x=><Link key={x.id} href={`/crm/campaigns/${x.id}`} className="flex items-center justify-between rounded-2xl border bg-white p-5"><div><h2 className="font-black">{x.name}</h2><p className="text-sm text-slate-500">{x.subject||'Subject not set'}</p></div><span className={`rounded-full px-3 py-1 text-xs font-black uppercase ${x.status==='ready'?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600'}`}>{x.status}</span></Link>)}</div>{!campaigns.length&&<p className="rounded-2xl border bg-white p-8 text-slate-600">No campaigns yet. Start with an audience and save a draft.</p>}</div>}
