import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';import{CampaignEditor}from'@/feature/marketing/components/CampaignEditor';import{CampaignDeliveryPanel}from'@/feature/marketing/components/CampaignDeliveryPanel';export default async function CampaignDetail({params}:{params:Promise<{id:string}>}){const{id}=await params,r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();const repo=r.composition.dependencies.marketing,[campaign,options,run]=await Promise.all([repo.getCampaign(id),repo.options(),r.composition.dependencies.marketingDelivery.getLatest(id)]);if(!campaign)notFound();const unavailable=r.composition.dependencies.marketingDeliveryProvider.mode==='unavailable';return <div className="space-y-5"><div><p className="text-xs font-black uppercase text-teal-600">{campaign.status}</p><h1 className="text-3xl font-black">{campaign.name}</h1></div>{unavailable&&<p role="alert" className="rounded-xl bg-amber-50 p-4 font-semibold text-amber-900">External marketing delivery provider is not configured. Campaign delivery is unavailable; no simulated fallback will be used.</p>}<CampaignDeliveryPanel campaignId={id} status={campaign.status} initialRun={run}/><CampaignEditor campaign={campaign} options={options}/></div>}
