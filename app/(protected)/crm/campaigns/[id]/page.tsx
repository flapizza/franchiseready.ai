import{notFound}from'next/navigation';import{resolveWorkspaceComposition}from'@/feature/platform/composition/resolveWorkspaceComposition';import{CampaignEditor}from'@/feature/marketing/components/CampaignEditor';export default async function CampaignDetail({params}:{params:Promise<{id:string}>}){const{id}=await params,r=await resolveWorkspaceComposition();if(r.status!=='resolved')notFound();const repo=r.composition.dependencies.marketing,[campaign,options]=await Promise.all([repo.getCampaign(id),repo.options()]);if(!campaign)notFound();return <div className="space-y-5"><div><p className="text-xs font-black uppercase text-teal-600">{campaign.status}</p><h1 className="text-3xl font-black">{campaign.name}</h1></div><CampaignEditor campaign={campaign} options={options}/></div>}
